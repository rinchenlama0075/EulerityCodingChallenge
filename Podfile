# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EulerityChallenge' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EulerityChallenge
  pod "AlamofireImage"
  pod "Parse"
  pod 'Brightroom/Engine', '2.0.0-alpha.8'
  pod 'Brightroom/UI-Classic', '2.0.0-alpha.8'
  pod 'Brightroom/UI-Crop', '2.0.0-alpha.8'


  target 'EulerityChallengeTests' do 
   inherit! :search_paths
    # Pods for testing
  end

  target 'EulerityChallengeUITests' do
    # Pods for testing
  end

end
